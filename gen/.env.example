# DEPRECATED — Use the root .env instead
# All environment variables are now managed from the project root:
#   cd /path/to/semana-ai-data-engineer
#   cp .env.example .env
#
# Docker Compose reads ../.env automatically.
# This file is kept for backwards compatibility only.
