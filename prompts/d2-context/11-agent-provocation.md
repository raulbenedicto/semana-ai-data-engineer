Today you manually chose when to use Qdrant vs Postgres.
But what if the agent could decide by itself?

Think about:
1. How would the agent know "faturamento" = SQL but "reclamacao" = semantic?
2. What if the question needs BOTH stores?
3. What if there were 3 agents, each specialized in one thing?

Tomorrow: the agent decides alone. LangChain + Chainlit + tool routing.
